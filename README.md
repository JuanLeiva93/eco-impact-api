### Cómo usar EcoCalculadora

Crea un archivo llamado .env en la carpeta principal.

Añade tu clave secreta: API_KEY=tu_clave_aquí

En index.html, asegúrate de que la variable MI_CLAVE coincida con la de tu .env.

Ejecuta el servidor con uvicorn main:app --reload.